machine:
  ruby:
    version:
      2.4.1

test:
  override:
    - bundle exec rspec:
        parallel: true
        files:
          - spec/**/*_spec.rb
          - lib/engines/shopping_cart/spec/**/*_spec.rb

# test:
#   override:
#     - bundle exec rspec spec lib/engines/shopping_cart/spec:
#         parallel: true
#         files:
#           - spec/unit/sample.rb   # can be a direct path to file
#           - spec/**/*.rb          # or a glob (ruby globs)
